[1mdiff --git a/Training/03_Mega_Research/03_treinar_gat.py b/Training/03_Mega_Research/03_treinar_gat.py[m
[1mindex eb36c7a..adbcd8b 100644[m
[1m--- a/Training/03_Mega_Research/03_treinar_gat.py[m
[1m+++ b/Training/03_Mega_Research/03_treinar_gat.py[m
[36m@@ -28,6 +28,7 @@[m [mfrom pathlib import Path[m
 [m
 import torch[m
 import torch.nn.functional as F[m
[32m+[m[32mfrom sklearn.metrics import f1_score[m
 from torch_geometric.loader import DataLoader[m
 [m
 # ─── Caminhos ─────────────────────────────────────────────────────────────────[m
[36m@@ -45,7 +46,7 @@[m [mEPOCHS_DEFAULT     = 30[m
 LR_DEFAULT         = 0.005[m
 WEIGHT_DECAY       = 5e-4[m
 BATCH_SIZE         = 64[m
[31m-PATIENCE           = 7    # early stopping: épocas sem melhoria na val_acc[m
[32m+[m[32mPATIENCE           = 7    # early stopping: épocas sem melhoria no val_f1 (macro)[m
 [m
 VARIANTES = {[m
     "baseline":   {"peso_ce": [1.0, 1.0], "arquivo": "pesos_gat.pth"},[m
[36m@@ -112,17 +113,19 @@[m [mdef treinar_epoca(model, loader, optimizer, criterion, device) -> tuple:[m
 [m
 [m
 @torch.no_grad()[m
[31m-def avaliar(model, loader, device) -> tuple:[m
[32m+[m[32mdef avaliar(model, loader, device) -> float:[m
     model.eval()[m
[31m-    total_correct = 0[m
[31m-    total = 0[m
[32m+[m[32m    y_true, y_pred = [], [][m
     for data in loader:[m
         data = data.to(device)[m
         out, _ = model(data.x, data.edge_index, data.batch)[m
[31m-        preds  = out.argmax(dim=1)[m
[31m-        total_correct += int((preds == data.y.squeeze()).sum())[m
[31m-        total += data.num_graphs[m
[31m-    return total_correct / total if total > 0 else 0.0[m
[32m+[m[32m        preds  = out.argmax(dim=1).cpu().tolist()[m
[32m+[m[32m        labels = data.y.squeeze().cpu().tolist()[m
[32m+[m[32m        y_pred.extend(preds)[m
[32m+[m[32m        y_true.extend(labels if isinstance(labels, list) else [labels])[m
[32m+[m[32m    if not y_true:[m
[32m+[m[32m        return 0.0[m
[32m+[m[32m    return f1_score(y_true, y_pred, average="macro", zero_division=0)[m
 [m
 [m
 # ─── Treinamento de uma variante ──────────────────────────────────────────────[m
[36m@@ -154,20 +157,20 @@[m [mdef treinar_variante([m
         optimizer, mode="max", factor=0.5, patience=5[m
     )[m
 [m
[31m-    best_val_acc    = 0.0[m
[31m-    best_state      = None[m
[32m+[m[32m    best_val_f1    = 0.0[m
[32m+[m[32m    best_state     = None[m
     epochs_sem_melhora = 0[m
     t_inicio        = time.time()[m
 [m
     for epoch in range(1, epochs + 1):[m
         train_loss, train_acc = treinar_epoca(model, train_loader, optimizer, criterion, device)[m
[31m-        val_acc               = avaliar(model, val_loader, device)[m
[32m+[m[32m        val_f1                = avaliar(model, val_loader, device)[m
 [m
[31m-        scheduler.step(val_acc)[m
[32m+[m[32m        scheduler.step(val_f1)[m
 [m
[31m-        melhorou = val_acc > best_val_acc[m
[32m+[m[32m        melhorou = val_f1 > best_val_f1[m
         if melhorou:[m
[31m-            best_val_acc       = val_acc[m
[32m+[m[32m            best_val_f1        = val_f1[m
             best_state         = {k: v.clone() for k, v in model.state_dict().items()}[m
             epochs_sem_melhora = 0[m
         else:[m
[36m@@ -175,14 +178,14 @@[m [mdef treinar_variante([m
 [m
         marker = " <- melhor" if melhorou else ""[m
         print(f"  Epoch {epoch:03d} | Loss: {train_loss:.4f} | "[m
[31m-              f"Train Acc: {train_acc:.4f} | Val Acc: {val_acc:.4f}{marker}")[m
[32m+[m[32m              f"Train Acc: {train_acc:.4f} | Val F1: {val_f1:.4f}{marker}")[m
 [m
         if epochs_sem_melhora >= PATIENCE:[m
             print(f"  [Early stopping] {PATIENCE} épocas sem melhoria. Parando.")[m
             break[m
 [m
     t_total = time.time() - t_inicio[m
[31m-    print(f"\n  Melhor Val Acc: {best_val_acc:.4f} | Tempo total: {t_total:.1f}s")[m
[32m+[m[32m    print(f"\n  Melhor Val F1 (macro): {best_val_f1:.4f} | Tempo total: {t_total:.1f}s")[m
 [m
     # Restaura os melhores pesos[m
     if best_state is not None:[m
