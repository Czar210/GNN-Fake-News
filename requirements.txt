# ──────────────────────────────────────────────────────────────────────
# Dependencias do projeto -- versoes pinadas a partir de um pip freeze
# real do ambiente em que os experimentos foram rodados.
# Para a API isolada (deploy minimo), ver requirements.api.txt.
# ──────────────────────────────────────────────────────────────────────
# Nota de reprodutibilidade: torch + torch_geometric sao sensiveis a
# combinacao OS/CUDA. Se voce esta em CPU-only, basta `pip install` direto.
# Para CUDA, siga as instrucoes oficiais do PyG (wheels pre-compilados):
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
# ──────────────────────────────────────────────────────────────────────

# Numericos / dados
numpy==2.4.2
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0

# Visualizacao
matplotlib==3.10.8
seaborn==0.13.2
pyvis==0.3.2

# Grafos
networkx==3.6.1
torch==2.10.0
torch-geometric==2.7.0

# NLP / embeddings
sentence-transformers==5.2.3
transformers==5.2.0
tokenizers==0.22.2
huggingface_hub==1.4.1
safetensors==0.7.0

# Utilitarios
tqdm==4.67.3
python-dotenv==1.2.1
requests==2.32.5

# Bluesky (legacy pipeline -- ver Training/01_BlueSky_Pipe/)
atproto==0.0.65

# API (FastAPI + SQLAlchemy)
fastapi==0.133.0
uvicorn==0.41.0
SQLAlchemy==2.0.46
pydantic==2.12.5

# Dev / formatadores (opcional)
black==26.1.0
flake8==7.3.0
